{
	"pluginAlias": "PalGatePlatform",
	"platform": true,
	"pluginType": "platform",
	"singular": true,
	"customUi": true,
	"headerDisplay": "PalGate Platform for Homebridge",
	"footerDisplay": "Created by @Knilo. \n\nAll config details can be obtained using `node palGateCli.js config` command.",
	"schema": {
		"type": "object",
		"properties": {
			"name": {
				"title": "Platform Name",
				"description": "The name of the platform instance.",
				"type": "string",
				"default": "PalGate Platform"
			},
			"token": {
				"title": "Session Token",
				"description": "The session token generated during device linking.",
				"type": "string"
			},
			"phoneNumber": {
				"title": "Phone Number",
				"description": "Your account's phone number (e.g., 972500000000).",
				"type": "string"
			},
			"tokenType": {
				"title": "Token Type",
				"description": "How this Homebridge instance is linked. SMS uses a one-time code; each PalGate account supports two device link slots (1 and 2).",
				"type": "number",
				"oneOf": [
					{ "title": "SMS", "enum": [0] },
					{ "title": "Linked Device 1", "enum": [1] },
					{ "title": "Linked Device 2", "enum": [2] }
				]
			},
			"accessoryType": {
				"title": "Default Accessory Type",
				"description": "The default type for discovered gates. Default is 'garageDoor'.",
				"type": "string",
				"default": "garageDoor",
				"oneOf": [
					{
						"title": "Garage Door",
						"enum": [
							"garageDoor"
						]
					},
					{
						"title": "Switch",
						"enum": [
							"switch"
						]
					},
					{
						"title": "Lock",
						"enum": [
							"lock"
						]
					}
				]
			},
			"gateOpeningDelay": {
				"title": "Gate Opening Duration (ms)",
				"description": "How long in milliseconds the gate shows as 'Opening' before becoming fully 'Open'. Default: 1000ms.",
				"type": "integer",
				"default": 1000,
				"minimum": 0
			},
			"gateCloseDelay": {
				"title": "Gate Close Duration (ms)",
				"description": "How long in milliseconds the gate stays in the fully 'Open' state before closing automatically. Default: 5000ms.",
				"type": "integer",
				"default": 5000,
				"minimum": 0
			},
			"enableRelayLocks": {
				"title": "Enable Relay Mode Accessories",
				"description": "Expose two virtual relay accessories (Hold Open / Hold Closed) for gates where you have admin/latching permissions.",
				"type": "boolean",
				"default": false
			},
			"relayAccessoryType": {
				"title": "Relay Accessory Type",
				"description": "The accessory type for virtual relay controllers (Hold Open / Hold Closed). Only applies when Relay Mode is enabled.",
				"type": "string",
				"default": "lock",
				"oneOf": [
					{
						"title": "Lock",
						"enum": [
							"lock"
						]
					},
					{
						"title": "Switch",
						"enum": [
							"switch"
						]
					}
				]
			},
			"pollInterval": {
				"title": "Poll Interval (Seconds)",
				"description": "How often in seconds the plugin should check the PalGate API for external state updates (e.g. Hold Open toggled by another admin). Default is 60.",
				"type": "integer",
				"default": 60,
				"minimum": 10
			},
			"customGates": {
				"title": "Custom Gate Settings",
				"description": "Optional overrides for individual gates. Each object in this array can specify custom settings for a gate.",
				"type": "array",
				"required": false,
				"items": {
					"type": "object",
					"properties": {
						"deviceId": {
							"title": "Gate ID",
							"description": "The unique ID of the gate.",
							"type": "string"
						},
						"name": {
							"title": "Custom Gate Name",
							"description": "A custom name for the gate.",
							"type": "string"
						},
						"garageDoor": {
							"title": "Expose as Garage Door",
							"description": "Set to true to expose this gate as a garage door.",
							"type": "boolean"
						},
						"switch": {
							"title": "Expose as Switch",
							"description": "Set to true to expose this gate as a switch.",
							"type": "boolean"
						},
						"lock": {
							"title": "Expose as Lock",
							"description": "Set to true to expose this gate as a lock.",
							"type": "boolean"
						},
						"hide": {
							"title": "Hide Gate",
							"description": "Set to true to hide this gate from HomeKit.",
							"type": "boolean"
						},
						"gateOpeningDelay": {
							"title": "Gate Opening Duration Override",
							"description": "How long in milliseconds this specific gate should show as 'Opening'. Defaults to global setting.",
							"type": "integer",
							"minimum": 0
						},
						"gateCloseDelay": {
							"title": "Gate Close Duration Override",
							"description": "How long in milliseconds this specific gate stays 'Open'. Defaults to global setting.",
							"type": "integer",
							"minimum": 0
						},
						"relayEnabled": {
							"title": "Enable Relay for this Gate",
							"description": "When disabled, relay accessories will not be created for this gate even if Relay Mode is globally enabled. Defaults to true.",
							"type": "boolean",
							"default": true
						},
						"relaySwitch": {
							"title": "Expose Relay as Switch",
							"description": "Set to true to expose the virtual relays for this gate as Switches.",
							"type": "boolean"
						},
						"relayLock": {
							"title": "Expose Relay as Lock",
							"description": "Set to true to expose the virtual relays for this gate as Locks.",
							"type": "boolean"
						}
					}
				}
			}
		},
		"required": [
			"token",
			"phoneNumber",
			"tokenType",
			"accessoryType",
			"gateOpeningDelay",
			"gateCloseDelay",
			"enableRelayLocks",
			"relayAccessoryType",
			"pollInterval"
		]
	}
}